# what-im-doing.txt — live status (fast-changing)

Update every meaningful step: current stage, what's running, what's blocked.
Keep in lockstep with context.txt.

## Stage
0.1.4 PUBLISHED (2026-07-18). Integrated four worker-agent branches into main
and released. Still NOT run in Factorio (M0 in-game verify pending — only JJ
can launch the game).

## Done (0.1.4)
- Merged (file-level) from the employee branches:
  - E1: crash fix on rocket launch (reads the cargo pod, not the removed 2.0
    rocket inventory) + guarded character edits.
  - E2: rocket-silo gate trimmed to ~10 "mature base" techs (JJ's choice);
    starting inventory +20.
  - E4: better placeholder icons + a survey-satellite icon + thumbnail; icon
    generator rewrite. Repointed survey-satellite.lua to the new icon.
  - E3: orbital science design (design/orbit-and-orbital-science.md) +
    prototypes/orbital-science.lua, left UN-WIRED until M0 verifies.
- Refreshed info.json description + PORTAL.md (today-vs-planned split).
- All Lua passes luac -p; zip built + published.

## Open / MUST verify in-game (M0)
- Nothing has run in Factorio. Load 0.1.4, watch for errors, run
  /jjt-new-planet, /jjt-goto <name>, /jjt-orbit.
- Science-pack yield: JJ reported red/green craft 1, not 2. Code looks correct
  (both audits) and E2 kept it; UNRESOLVED — re-check on 0.1.4, then diagnose
  or remove.

## Next (lanes)
- MAIN (this session): planet terrain per class ("barren Xavara is grassy") +
  new custom tiles.
- Employees: break-blueprint restructure (jjt- early intermediates/buildings);
  the original Core Tap extraction mechanic.
- After M0 verifies: wire orbital-science.lua; real orbit ascent (PLAN M1/M2).

## Coordination
- MANAGER-COMMS.txt is the channel with MANAGER-CLAUDE. Releases are
  centralized through MAIN (this session). Employees work on branches; MAIN
  reviews + merges + publishes.

Last updated: 2026-07-18
