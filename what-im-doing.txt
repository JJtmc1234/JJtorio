# what-im-doing.txt — live status (fast-changing)

Update every meaningful step: current stage, what's running, what's blocked.
Keep in lockstep with context.txt.

## Stage
0.1.1 staged (NOT yet published): added QoL tweaks (reach), ~40% cheaper
research, and rocket-silo TECH gating behind late-game techs. Foundation
+ planet core from 0.1.0. Still NOT run in Factorio.

## Done
- Mod moved into mod/ subfolder; repo root keeps design docs.
- Junction at %APPDATA%\Factorio\mods\JJtorio repointed to mod/.
- data stage: early-game mining-speed tweak (prototypes/tweaks.lua).
- control stage: storage init + procedural planet generation
  (scripts/planet-gen.lua) + dev commands (scripts/commands.lua).
- PUBLISHED 0.1.0 to the mod portal (first publish via website by user).
- Auto-publisher live: tools/publish.ps1 uploads via the portal API using
  FACTORIO_API_KEY (set as a user env var; confirmed visible to the shell).
  Claude can push future versions with publish.ps1 -NewVersion x.y.z.
- Dev tooling: tools/build-release.ps1, tools/gen-placeholder-icons.ps1,
  placeholder icons under mod/graphics/icons/, README, .gitattributes.
- All Lua passes luac -p (syntax only; Factorio API unverified).

## Decided
- SE-style procedurally generated planets (runtime surfaces).
- Light early game; depth in space. Keep the vanilla rocket silo.
- Incompatible with SA and SE; standalone overhaul.
- Publish to the mod portal as a sync/test path (user accepts it's public).
  Holding the actual public upload until the user is back — not firing an
  irreversible public action unattended.

## Open questions / MUST verify in-game
- UNVERIFIED: the whole mod has not been loaded in Factorio yet. Code
  follows the documented 2.0 API but nothing is confirmed to run.
  Verify: load save, run /jjt-new-planet, /jjt-goto <name>; watch for errors.
- Confirm game.create_surface + MapGenSettings behave as assumed on base 2.0.
- Confirm `storage` (not `global`) and command re-registration on load.

## Next steps (roadmap)
1. Load in-game, fix whatever the API got wrong (expect some). [USER]
   Then ship the fix as 0.1.1 via publish.ps1.
2. Real travel: tie planet arrival to the rocket silo instead of /jjt-goto.
3. Give planets distinct terrain per class (tiles/autoplace), not just
   recolored nauvis.
4. Make planet "facts" mechanically matter (gravity, day length).

## Blocked
- Step 1 needs an in-game test run (only the user can launch Factorio).

Last updated: 2026-06-13
